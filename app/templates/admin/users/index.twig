{% extends 'admin/dashboard-layout.twig' %}

{% block content %}
  {% if users %}
  <ul>
    {% for user in users %}
      <li>{{ user.username }}</li>
    {% endfor %}
  </ul>
  {% endif %}
{% endblock %}
